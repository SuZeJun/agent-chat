// Package ticket 定义工单草稿、人工确认状态机与工单记录。
//
// 该领域承载四条不可协商的写操作安全属性（docs/EVALUATION.md 7.3）：
//
//   - 未确认不执行写操作
//   - 取消后不执行写操作
//   - 过期确认不执行写操作
//   - 重复确认不产生重复副作用
//
// 前三条由审批状态机保证：只有 pending 能转为 approved，且转换时校验未过期。
// 第四条由两层保证：状态转换本身只能成功一次，工单再以服务端派生的幂等键唯一。
package ticket

// Package agenttool 定义 Agent 可用的工具及其显式白名单。
//
// 两条不可协商的约束：
//
//   - 工具的授权作用域由服务端在构造时绑定，不接受来自模型输出的标识。模型能
//     影响的只有业务参数，无法通过构造参数读取其他客户的数据。
//   - 只有注册表中的工具可被调用。模型给出的任何其他名称都会被拒绝，而不是
//     尝试解析或近似匹配。
package agenttool
